{
  "name": "cicd-demo-task-tracker",
  "version": "1.0.0",
  "description": "Simple static Task Tracker app used as a CI/CD pipeline demo (Continuous Integration, Testing, Inspection, Deployment) for Tugas Besar Manajemen Konfigurasi dan Evolusi PL.",
  "main": "src/app.js",
  "scripts": {
    "build": "node build.js",
    "test": "jest --coverage",
    "lint": "eslint src tests",
    "start": "npx serve dist"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "collectCoverageFrom": [
      "src/**/*.js"
    ],
    "coverageDirectory": "coverage"
  },
  "license": "MIT"
}
